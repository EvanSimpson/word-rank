if avgWords
  .panel.panel-default
    .panel-heading
      h2.panel-title Average Word Count
    .panel-body= avgWords.avgWords + ' words'
if totalTime
  .panel.panel-default
    .panel-heading
      h2.panel-title Scan duration
    .panel-body= totalTime.total_time + ' seconds'
